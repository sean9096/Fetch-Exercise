To run the application Docker must be installed:

1. Build Docker image via command: "docker build . -t takehome"

2. After image has successfully built run docker image via command: "docker run -it -p 8080:8080 takehome"

To test application:

1. POST to endpoint "http://localhost:8080/receipts/process" with a json resembling the receipt the result will be a generated id

2. GET the points via "http://localhost:8080/receipts/{id}/points" using the id recieved in the first step

!!!THANK YOU FOR YOUR TIME AND CONSIDERATION :D!!!